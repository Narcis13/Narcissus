{
  "dependencies": {
    "@hono/zod-validator": "^0.5.0",
    "googleapis": "^150.0.1",
    "hono": "^4.7.8",
    "hono-sessions": "^0.7.2",
    "zod": "^3.24.4"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --out dist/index.js",
    "lint": "echo 'No linter configured'",
    "test": "echo 'No tests specified'"
  },
  "type": "module",
  "engines": {
    "bun": ">=1.0.0"
  },
  "author": "",
  "license": "MIT",
  "description": "Bun + Hono API server"
}
